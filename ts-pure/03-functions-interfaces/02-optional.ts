function geetHell(name: string, message?: string): string {
  if (message) {
    return `${name}, ${message}`;
  }
  return `Hello, ${name}`;
}

console.log('Jhack');
console.log('Jack', 'How are you');